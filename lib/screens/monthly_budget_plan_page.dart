import 'package:flutter/material.dart';

class MonthlyBudgetPlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Hapus AppBar di sini
      body: Center(child: Text('Here is your monthly budget plan!')),
    );
  }
}
